function selectOption(option) {
    alert("You selected " + option + "!");
    // Here you can redirect to a specific page or load content dynamically
}

function navigateTo(section) {
    alert('Navigating to ' + section);
}